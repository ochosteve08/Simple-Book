const { db } = require('../index')
const { errorHandler } = require('../utils/errorHandler')

const listOrderItems = async (req, res, next) => {}

const deleteOrderItem = async (req, res, next) => {}

module.exports = { listOrderItems, deleteOrderItem }
